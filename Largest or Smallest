#include<stdio.h>  

int main()   
{
int a, b, c, l, s;

printf("Enter the numbers:") ;   
 
scanf("%d %d %d", &a, &b, &c) ;  

l=a>b && a>c? a:(b>c? b:c);

s=a<b && a<c? a:(b<c? b:c);

printf("\n Largest: %d", l) ;   

printf("\n Smallest: %d", s) ;   

return 1;
}
