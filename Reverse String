#include <stdio.h>  
#include <string.h>  
int main( )
{
 char a[50];
 int i, n;
 printf("Enter string: ");
 scanf("%s", a);
 n=strlen(a);
 printf ("\n \n Reverse string: "); 
 for(i=n-1; i>=0;  i--) 
 {
  printf("%c" , a[i]);
 }
return 1;
} 
